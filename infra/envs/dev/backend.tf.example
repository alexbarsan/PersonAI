terraform {
  backend "s3" {
    bucket         = "REPLACE_WITH_DEV_TERRAFORM_STATE_BUCKET"
    key            = "dreamlens/dev/terraform.tfstate"
    region         = "REPLACE_WITH_AWS_REGION"
    dynamodb_table = "REPLACE_WITH_DEV_TERRAFORM_LOCK_TABLE"
    encrypt        = true
  }
}
