terraform {
  backend "s3" {
    bucket         = "REPLACE_WITH_PROD_TERRAFORM_STATE_BUCKET"
    key            = "dreamlens/prod/terraform.tfstate"
    region         = "REPLACE_WITH_AWS_REGION"
    dynamodb_table = "REPLACE_WITH_PROD_TERRAFORM_LOCK_TABLE"
    encrypt        = true
  }
}
