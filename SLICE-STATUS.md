# Slice Status

Status values: `Not started`, `In progress`, `Blocked`, `Done`.

| Slice | Status | Date completed | Commit | Verification | Notes | Next Step |
| --- | --- | --- | --- | --- | --- | --- |
| S0 | Done | 2026-06-30 | S0 commit | `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Normalized monorepo foundation, CI skeleton, and plan docs under `docs/plan/`. | Build S1 walking skeleton API. |
| S1 | Done | 2026-07-01 | S1 commit | `dotnet test api/DreamLens.sln --configuration Release` | Added .NET 9 API solution, health endpoints, development OpenAPI, Dockerfile, CI API test step, and unit/integration test projects. Docker image build was attempted but skipped because the Docker daemon is not running. | Start S2: add PostgreSQL, EF Core 9, migrations, and Testcontainers integration fixture. |
| S2 | Done | 2026-07-01 | S2 commit | `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Added PostgreSQL EF Core 9 foundation, initial migration, DbContext, readiness probing, and Testcontainers PostgreSQL fixture. Docker-backed tests skip locally when the Docker daemon is unavailable; CI now runs on Ubuntu for Docker support. | Start S3: add Cognito JWT validation, local dev/test tokens, `ICurrentUser`, and `/v1/me`. |
| S3 | Done | 2026-07-01 | S3 commit | `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Added JWT bearer/Cognito auth wiring, testing-only auth scheme, `ICurrentUser`, and `/v1/me`. Test auth headers are ignored outside the `Testing` environment. | Start S4: add encrypted user profile, traits, consent flags, and profile endpoints. |
| S4 | Done | 2026-07-01 | S4 commit | `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Added encrypted user profile storage, traits, consent flags, profile endpoints, and EF migration. Sensitive traits are encrypted at rest. | Start S5: add PersonaKit `IChatClient` DeepSeek adapter, fake chat client, resilience/logging decorators. |
| S5 | Done | 2026-07-01 | S5 commit | `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Added PersonaKit project, `IChatClient` DeepSeek adapter, fake chat client, Polly resilience decorator, usage logging decorator, and DreamLens API registration. | Start S6: add persona config loading, prompt rendering, and schema validation. |
| S6 | Done | 2026-07-01 | S6 commit | `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Added PersonaKit persona registry, strict Scriban prompt renderer, JsonSchema.Net output validator, DreamLens persona assets, and Verify prompt snapshot coverage. | Start S7: add pseudonymized Context JSON builder. |
| S7 | Done | 2026-07-01 | S7 commit | `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Added PersonaKit Context JSON v1 builder, HMAC pseudonym service, consent-gated traits/history, dream input validation/capping, and snapshot coverage. | Start S8: add interpretation pipeline orchestration. |
| S8 | Done | 2026-07-01 | S8 commit | `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Added PersonaKit interpretation pipeline orchestration, section mapping, repair retry, friendly failures, no-op moderation, and in-memory run/interpretation stores. | Start S9: add dream submit and get endpoints. |
| S9 | Done | 2026-07-01 | S9 commit | `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Added `POST /v1/dreams` and `GET /v1/dreams/{id}`, dream persistence/migration, pipeline wiring, ownership checks, and friendly failure handling. | Start S10: add journal and insights endpoints. |
| S10 | Done | 2026-07-01 | S10 commit | `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Added current-user dream journal listing, dream deletion, insights for recurring themes and streaks, and user isolation coverage. | Start S11: add rate limits, quotas, abuse protection, and AI cost ledger. |
| S11 | Done | 2026-07-01 | S11 commit | `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Added global per-user/IP rate limiting, daily dream quotas, oversized dream rejection, AI cost ledger persistence, EF migration, and tests for safe 429 responses and no raw dream text in ledger rows. | Start S12: scaffold Expo app with router, theme, API client, auth, and mock mode. |
| S12 | Done | 2026-07-01 | S12 commit | `npm test` in `app/`; `npm run typecheck` in `app/`; `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Scaffolded Expo React Native app with Expo Router, theme/brand provider, TanStack Query, Zustand auth and draft state, typed API client, MSW mock handlers, RNTL/Jest setup, and deterministic mock mode. Expo web is running locally on port 8081. | Start S13: add onboarding wizard and profile UI. |
| S13 | Done | 2026-07-01 | S13 commit | `npm test` in `app/`; `npm run typecheck` in `app/`; `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Added onboarding/profile routes, React Hook Form + zod profile form, consent controls, profile query/mutation wiring, mock-mode profile saving, local onboarding draft state, and disclaimer coverage. | Start S14: add dream capture and result screens. |
| S14 | Done | 2026-07-01 | S14 commit | `npm test` in `app/`; `npm run typecheck` in `app/`; `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Added dream capture and result routes, capture validation, submit/loading/error handling, result cache, generic section renderer, result disclaimer, and elevated-safety constrained UI. | Start S15: add journal and insights UI. |
| S15 | Done | 2026-07-01 | S15 commit | `npm test` in `app/`; `npm run typecheck` in `app/`; `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1` | Added journal list/detail routes, journal empty state, delete action, insights route, theme/streak summaries, query wiring, mock handlers, and UI tests. | Start S16: add Maestro and Playwright E2E harness. |
| S16 | Done | 2026-07-01 | S16 commit | `npm test` in `app/`; `npm run typecheck` in `app/`; `npm run e2e:web` in `app/`; `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1`; `maestro --version` blocked locally because Maestro is not installed. | Added Playwright web E2E happy/error paths, Maestro mobile flow, stable UI test IDs, cross-platform Playwright config, app CI checks, and fixed Expo Router root by moving helper code out of `src/app`. | Start S17: add Terraform AWS infrastructure. |
| S17 | Done | 2026-07-02 | S17 commit | `powershell -ExecutionPolicy Bypass -File scripts/check-s17-infra.ps1`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1`; `terraform fmt -check -recursive infra` blocked locally because Terraform is not installed. | Added Terraform AWS infrastructure layout with dev/prod environment roots, reusable modules for VPC, ECS Fargate API, ALB/WAF, RDS PostgreSQL, Cognito, S3/CloudFront web hosting, Secrets Manager/KMS, GitHub OIDC role foundation, and observability placeholders. Remote state remains a documented backend placeholder and no real secrets are committed. | Start S18: add CI/CD pipelines for API, web, mobile, and infra. |
| S18 | Done | 2026-07-02 | S18 commit | `powershell -ExecutionPolicy Bypass -File scripts/check-s18-workflows.ps1`; `powershell -ExecutionPolicy Bypass -File scripts/check-s17-infra.ps1`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1`; `npm test` in `app/`; `npm run typecheck` in `app/`; `npm run build:web` in `app/`; `dotnet test api/DreamLens.sln --configuration Release`; `docker build -f api/src/DreamLens.Api/Dockerfile api` blocked locally because the Docker daemon is not running. | Added GitHub Actions deployment workflows for API container/ECS rollout, Expo web S3/CloudFront deploy, Terraform validate/plan/apply, and manual EAS mobile builds. Documented required GitHub environments, OIDC variables, EAS token, and Terraform state bootstrap. Local Expo web export required elevated permissions because Expo could not write generated `.expo/dev/logs` files in the sandbox. | Start S19: add observability, hardening, and k6 load tests. |
| S19 | Done | 2026-07-02 | S19 commit | `dotnet test api/DreamLens.sln --configuration Release`; `powershell -ExecutionPolicy Bypass -File scripts/check-s19-observability.ps1`; `powershell -ExecutionPolicy Bypass -File scripts/check-s18-workflows.ps1`; `powershell -ExecutionPolicy Bypass -File scripts/check-s17-infra.ps1`; `powershell -ExecutionPolicy Bypass -File scripts/check-s0-structure.ps1`; `k6 version` blocked locally because k6 is not installed. | Added built-in .NET metrics for AI cost/tokens, quota rejections, rate-limit rejections, and provider failures; hardened API response headers; added log-redaction regression coverage for dream text; added ADOT collector config, k6 smoke script, CloudWatch dashboard/alarms, observability runbook, and CI static gate. Terraform validation remains blocked locally because Terraform is not installed. | Start S20: prove PersonaKit reuse with Astra config. |
| S20 | Done | 2026-07-02 | S20 commit | `dotnet test api/DreamLens.sln --configuration Release`; `npm test` in `app/`; `npm run typecheck` in `app/`; `powershell -ExecutionPolicy Bypass -File scripts/check-s20-astra.ps1` | Added config-only Astra reuse proof with `astrologer` persona metadata, prompt, output schema, section map, stable prompt snapshot, schema validation, generic section mapping, and pipeline coverage using existing PersonaKit abstractions. Added app-side Astra brand variant selected by `DREAMLENS_APP_VARIANT=astra`; DreamLens remains default. | Start S21: decide whether monetization is in v1, then add tiers/paywall if approved. |
| S21 | Done | 2026-07-02 | S21 commit | `dotnet test api/DreamLens.sln --configuration Release`; `npm test` in `app/`; `npm run typecheck` in `app/`; `powershell -ExecutionPolicy Bypass -File scripts/check-s21-monetization.ps1` | Added mock-first monetization foundation without third-party payment subscriptions: entitlement service, `/v1/entitlements`, quota-by-tier enforcement, free/premium tier configuration, mock paywall route, entitlement UI state, tests, docs, and CI static gate. Real purchase validation remains blocked until RevenueCat/App Store/Google Play setup exists. | Use `docs/plan/13-post-s21-feature-backlog.md` to choose the next product slice; continue AWS production readiness in parallel. |
